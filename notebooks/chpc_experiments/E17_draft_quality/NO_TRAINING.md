# E17_draft_quality — no `train.ipynb`

This experiment does **🏁 **CLOSED 2026-08-07** — no training was ever required. The artifacts in this
folder are the completed record: `score_all.py`, `run_local_models.py`, the probe CSVs, every
translator's output, `SCORES.csv`, and `COMPARISON.md`.

Re-score any new candidate with `python score_all.py` from this directory.
